language: cpp

matrix:
  include:
    - os: linux
      dist: xenial
      sudo: required
      compiler: clang
      addons:
        apt:
          packages:
          - cmake
          - clang
          - llvm
          - libfuse-dev
          - libfuse2
    - os: osx
      sudo: false
      osx_image: xcode8.2
      compiler: clang
      before_install:
        - brew update
        - brew cask install osxfuse

compiler:
  - clang
  - gcc

before_script:
  - mkdir build
  - cd build
  - cmake ..

script: make

notifications:
  email: false
language: cpp
sudo: false

matrix:
  include:
    - os: linux
      dist: trusty
      compiler: clang
      addons:
        apt:
          packages:
          - libfuse-dev
          - libfuse2
    - os: osx
      osx_image: xcode8.2
      before_install:
        - brew update
        - brew cask install osxfuse

before_script:
  - mkdir build
  - cd build
  - cmake ..

script: make

notifications:
  email: false